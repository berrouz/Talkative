package com.github.berrouz.common;

/**public interface SendableAsJson{
 * interface which defines a method toJson in order to be sent via network
 */
public interface SendableAsJson{
    String toJson();
}